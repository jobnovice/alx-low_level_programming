#include <stdio.h>
#include <"lists.h">

/**
 * size_t list - new function 
 * @h: a linked list
 * Return: the number of elements
 */

 size_t list_len(const list_t *h)
 {
    const list_t *temp;// temporary linked list
    size_t nbr_elem = 0;
    temp = h;
    while (temp != NULL)
    {
        
    }

 }
